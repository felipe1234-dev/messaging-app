import { Composer } from "@components";
import Page from "./Page";

function Home() {
    return (
        <Composer components={[]}>
            <Page />
        </Composer>
    );
}

export default Home;
